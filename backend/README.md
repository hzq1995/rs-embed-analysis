# FastAPI Backend

## Run

```powershell
uvicorn backend.app.main:app --reload
```

## Environment

- `EE_PROJECT`: Earth Engine project ID
- `EE_AUTH_MODE`: optional Earth Engine auth mode
- `ALLOWED_ORIGINS`: comma-separated frontend origins
